[{'with': 'unique session (jlibtorrent Session)\n    //   - unique listening port (utp + tcp)\n    //   - unique node_id (20 bytes', 'bootstrap': 'all nodes bootstrap off node 0\n    //\n    // Methods:\n    //   List<Session> getSessions()\n    //   Session getNode(int index)\n    //   void waitForDhtReady(Duration timeout)\n    //   void partition(int nodeA', 'NAT': 'drop packets between A↔B\n    //   void allow(int nodeA', 'notes**': 'Uses `jlibtorrent.Session` with `settings_pack` configured for DHT-only (no torrent storage)\n- Ports allocated from ephemeral range (49152–65535) via `ServerSocket(0)`\n- Bootstrap: node 0 starts first', 'session.addDhtNode("127.0.0.1': 'node0Port)`\n- `partition()` uses a packet-filter wrapper or simply closes the UDP socket between two sessions — simpler: each node gets its own `DatagramSocket` bound to its port; `partition(A', 'IP': 'port (localhost', 'nodes': 0, 'LocalDhtClusterExtension`': 'BeforeAll` starts cluster with N nodes\n- `@AfterAll` closes cluster\n- Provides `cluster` field to test class\n\n### 2.3 Test Scenarios (Each = One `@Test` Method)\n\n| Scenario | Description | NAT Topology |\n|----------|-------------|--------------|\n| `testIdentityPublishAndFetch` | Node publishes identity record; another node fetches it | All cone |\n| `testRelayPublishAndFetch` | Relay candidate publishes relay record; searcher fetches list | All cone |\n| `testDirectUtpSuccess` | Searcher → target via uTP hole-punch (no relay needed) | Both cone |\n| `testDirectUtpFailsThenRelay` | Hole-punch fails → fetch relay list → connect relay → forward query | Searcher=symmetric', 'Total**': 15, 'Key': 'SHA-256(', 'frostwire-identity-v1': 'node_id)[:32]`)\n\n```json\n{', 'v': 1, 'node_id': 'A1B2C3D4E5F6...', 'ed25519_pub': 'BASE64(32 bytes)', 'x25519_pub': 'BASE64(32 bytes)', 'utp_port': 49152, 'first_seen': 1749400000, 'last_seen': 1749403600, 'sig': 'BASE64(64 bytes)'}, 'Canonicalization**', 3.2, 'Key', 'SHA-256("frostwire-relay-nodes-v1', [32], 44, 'seq', 'Actually', 44, 'SHA-256("frostwire-relay-nodes-v1', [32], 32, 'simpler', 'get', 'v1**', 'decentralized', 'item)', {'v': 1, 'node_id': 'A1B2...', 'ed25519_pub': 'BASE64(...)', 'utp_port': 49152, 'first_seen': 1749400000, 'last_seen': 1749403600, 'relay_count': 47, 'max_qps': 5, 'hashcash': '0000a1f2...', 'sig': 'BASE64(64 bytes)"           // Ed25519 sig over canonical JSON without "sig'}, 3.3, {'v': 1, 'subject_id': 'A1B2...', 'issuer_id': 'ROOT1...', 'delegation_depth': 1, 'expiry': 1780936000, 'sig': 'BASE64(64 bytes)', 'validation**': 'BFS from hardcoded root keys. Accept subject if path exists with depth ≤ 3 and all packets unexpired.\n\n---\n\n## 4. Wire Protocols\n\n### 4.1 Publishing (both identity & relay records)\n\n```java\nvoid publishIdentity(Session s', 'Re-publish**': 'Timer every 5 min. `last_seen = now()`. Relay also recomputes `hashcash` each time (cost ~10-50ms).\n\n### 4.2 Discovery\n\n```java\n// Identity fetch (single)\nOptional<IdentityRecord> fetchIdentity(Session s', 'salt).map(IdentityRecord': 'parse);'}, {'filter(Optional': 'isPresent)\n        .map(Optional::get)\n        .map(RelayRecord::parse)\n        .filter(r -> r.last_seen > now() - 30min)\n        .sorted(Comparator.comparingInt(RelayRecord::relayCount))\n        .toList();'}, 4.3, 'R', {'nonce_A': 32, 'A': 'CHALLENGE_RESPONSE { nonce_A', 'nonce_R': 32, 'sig_R': 'Ed25519(nonce_A || nonce_R)'}, 'R', {'sig_A': 'Ed25519(x25519_pub_A || nonce_R)'}, 'A', {'sig_R': 'Ed25519(x25519_pub_R || nonce_A)'}, 'derive', 'frostwire-relay-transport-v1', 'traffic', 96, 4.4, 'computes', 'frostwire-search-v1', 'encrypts', 'R', {'target_node_id': 'B.node_id', 'B': 'RELAY_PAYLOAD { source_node_id: A.node_id', 'Verify': 'all identity tests pass\n\n### 5.3 Relay Record (Commit 3)\n\n- [ ] `RelayRecord` class (parse', 'com.frostwire.tests.dht.*': 'passes (all disabled)\n\n### 5.2 Identity Record (Commit 2)\n\n- [ ] `IdentityRecord` class (parse', 'tests': 'trust chain validation', 'test': 'testWoTTrustValidation`\n- [ ] Verify\n\n### 5.7 Relay Metrics & UI (Commit 7)\n\n- [ ] `RelayServer` increments `relay_count` per forwarded query\n- [ ] Expose via existing `SearchEngine` stats callback / Advanced Settings panel\n- [ ] Integration test: `testRelayCountMetricsExposed`\n- [ ] Verify\n\n### 5.8 DoS Protection (Commit 8)\n\n- [ ] `RelayServer` enforces `max_qps` per source node_id (token bucket)\n- [ ] Integration test: `testRelayQpsLimit`\n- [ ] Verify\n\n### 5.9 Full Flow Integration (Commit 9)\n\n- [ ] `testDirectUtpSuccess`\n- [ ] `testDirectUtpFailsThenRelay`\n- [ ] `testMultipleRelaysLoadBalance`\n- [ ] `testRePublishInterval`\n- [ ] Verify all 15 integration tests green\n\n### 5.10 Documentation & Changelog (Commit 10)\n\n- [ ] Update this design doc with any deviations\n- [ ] `desktop/changelog.txt`', 'Sybil': "attacker floods relay registry | Hashcash on relay record (4 leading zero bits = ~16 attempts avg); re-compute every 5 min |\n| DoS: attacker floods relay with queries | Per-source token bucket (`max_qps` default 5); relay drops excess |\n| Privacy: relay reads queries | End-to-end AES-GCM payload encryption; relay only sees target node_id |\n| Eclipse: attacker isolates victim from honest relays | WoT: relays must have valid trust chain from roots; victim only uses trusted relays |\n| Replay: attacker replays old challenge/response | Nonces (32 bytes random) + sequence numbers on transport |\n| Identity theft: attacker claims victim's node_id | Ed25519 sig on identity/relay records; WoT binds key to node_id |\n\n---\n\n## 7. Configuration Constants\n\n```java\n// All in com.frostwire.search.relay.RelayConstants\nint RELAY_REGISTRY_TTL_SEC = 30 * 60;           // 30 min\nint RELAY_REPUBLISH_INTERVAL_SEC = 5 * 60;      // 5 min\nint IDENTITY_REPUBLISH_INTERVAL_SEC = 5 * 60;\nint HASHCASH_LEADING_ZERO_BITS = 4;             // adjustable\nint DEFAULT_MAX_QPS = 5;\nint WOT_MAX_DEPTH = 3;\nint CHALLENGE_NONCE_BYTES = 32;\nint TRANSPORT_NONCE_BYTES = 12;                 // AES-GCM 96-bit\nint PAYLOAD_NONCE_BYTES = 12;\nString DHT_IDENTITY_PREFIX =", 'frostwire-relay-nodes-v1";\nString TRANSPORT_KDF_LABEL = "frostwire-relay-transport-v1";\nString PAYLOAD_KDF_LABEL = "frostwire-search-v1': '--', 'org.junit.jupiter': 'junit-jupiter` (already in project)\n\n---\n\n## 9. Out of Scope for Phase 4a\n\n- Onion routing (Phase 5)\n- Search query/response protocol (Phase 3 — bloom filter exchange)\n- NAT hole-punch implementation (Phase 3 — uTP rendezvous)\n- Android UI for relay metrics (separate commit after desktop)\n\n---\n\n## 10. Review Checklist\n\nBefore implementation starts', 'confirm': ['Test infrastructure approach (LocalDhtCluster) is feasible with jlibtorrent API\n- [ ] Hashcash difficulty (4 bits) is reasonable for desktop CPU\n- [ ] WoT root keys: which 3-5 FrostWire release-signing ed25519 pubkeys?\n- [ ] Identity persistence path: `~/.frostwire/identity.dat` (desktop) / app-private dir (android)\n- [ ] Relay metrics UI location: Advanced Settings → "Relay Stats', 'panel\n- [ ] Any missing threat vectors?\n\n---\n\n*End of design doc. Next step: review, then Commit 1 (test infrastructure).*']}]